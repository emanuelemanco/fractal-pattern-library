<nav id="navigation-primary" class="navigation-primary" tabindex="-1">

    <ul class="">
        {{#each menu.items}}
            <li>
                <a href="{{path this.link}}">
                {{this.label}}
                </a>
                {{#if items}}
                    <ul class="">
                        {{#each items}}
                            <li>
                                <a href="{{path this.link}}">
                                    {{this.label}}
                                </a>
                            </li>
                        {{/each}}
                    </ul>
                {{/if}}
            </li>
        {{/each}}
    </ul>

</nav>
